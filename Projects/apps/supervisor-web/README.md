# Supervisor Web Dashboard

Static supervisor dashboard for SiteSnap.

## Run locally

1. Ensure API is running on `http://localhost:4000` (or set another URL in the login form).
2. Serve this folder with any static server:

```bash
cd apps/supervisor-web
npx serve .
```

Then open the served URL in your browser and sign in with a supervisor/admin account.
