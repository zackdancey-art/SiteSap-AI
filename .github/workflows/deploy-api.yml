name: Deploy API

on:
  push:
    branches: ["main", "master"]
    paths:
      - "Projects/services/api/**"
      - "Projects/shared/**"
      - "Dockerfile"
      - ".dockerignore"

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Projects

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Get pnpm store path
        id: pnpm-cache
        run: echo "store=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.store }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('Projects/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared types
        run: pnpm --filter ./shared run build:types

      - name: Typecheck
        run: pnpm --filter ./services/api run typecheck

      - name: Tests
        run: pnpm --filter ./services/api run test

      # ── Container build & push ─────────────────────────────────────────────
      # Uncomment and fill in the registry/image name for your hosting provider.
      # The examples below show Google Artifact Registry; adapt for ECR, GHCR, etc.
      # Build context is the repo root so Dockerfile can reference Projects/.

      # - name: Authenticate to Google Cloud
      #   uses: google-github-actions/auth@v2
      #   with:
      #     credentials_json: ${{ secrets.GCP_SA_KEY }}

      # - name: Configure Docker for GAR
      #   run: gcloud auth configure-docker REGION-docker.pkg.dev

      # - name: Build and push
      #   working-directory: ${{ github.workspace }}
      #   run: |
      #     docker build \
      #       -t REGION-docker.pkg.dev/PROJECT/REPO/sitesnap-api:${{ github.sha }} \
      #       -t REGION-docker.pkg.dev/PROJECT/REPO/sitesnap-api:latest \
      #       .
      #     docker push --all-tags REGION-docker.pkg.dev/PROJECT/REPO/sitesnap-api

      # - name: Deploy to Cloud Run
      #   run: |
      #     gcloud run deploy sitesnap-api \
      #       --image REGION-docker.pkg.dev/PROJECT/REPO/sitesnap-api:${{ github.sha }} \
      #       --region REGION \
      #       --platform managed \
      #       --allow-unauthenticated \
      #       --set-env-vars NODE_ENV=production
